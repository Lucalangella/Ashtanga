//
//  DifficultyDotsView.swift
//  Ashtanga
//
//  Created by Luca Langella 1 on 13/12/24.
//

import SwiftUI

/* struct DifficultyDotsView: View {
    let difficulty: Int

    var body: some View {
        HStack(spacing: 4) {
            ForEach(1...5, id: \.\self) { index in
                Circle()
                    .fill(index <= difficulty ? Color.blue : Color.gray.opacity(0.3))
                    .frame(width: 8, height: 8)
            }
        }
    }
}

#Preview {
    DifficultyDotsView()
}
*/
